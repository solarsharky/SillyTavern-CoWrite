# Changelog

所有重要变更都会记录在这里。本项目遵循 [Semantic Versioning](https://semver.org/)。

## [0.1.0-beta.12] - 2026-09-03

### Changed

- 默认长记录预算提高到 120,000 tokens，上限提高到 1,000,000 tokens；设置框明确改名为“自动压缩阈值”，由用户自行调高或调低来决定何时压缩。
- 旧版 12,000 默认预算升级一次，保留其他自定义预算及升级后的手动调整；备份恢复也应用同一升级规则。

### Fixed

- 移除生成前隐藏的 `min(12,000, 酒馆上下文 × 40%, 设置预算)` 限制，修复调高预算后仍无法让 Char 点评原问卷的问题。
- 无法摘要时明确显示记录 token 数及实际使用的预算；保存上下文设置后，原记录下一次生成直接使用新值。

## [0.1.0-beta.11] - 2026-09-03

### Changed

- 双人问卷和 Char 给 User 的问卷默认每组 10 题，首次出题与追加题均使用新默认。
- 内置双人“依恋类型”改为 10 题量表测试，要求双方独立作答；逐题点评后给出各自得分、依恋类型倾向、答案依据和相处建议。
- 自动升级未修改的旧版默认题数与依恋内容项，保留用户自定义内容、已删除的内容项和已有记录的内容要求。

## [0.1.0-beta.10] - 2026-09-03

### Fixed

- 修复通过 HTTP 局域网地址访问时，浏览器缺少 `crypto.randomUUID()` 导致新建问卷等操作报错的问题。
- 记录、轮次、卡片、生成请求、模板、内容项、API 连接及导入统一使用兼容的 UUID 生成方法；原生接口不可用时通过 `crypto.getRandomValues()` 生成标准 v4 UUID。

## [0.1.0-beta.9] - 2026-09-03

### Added

- 每个 API 连接新增流式／非流式输出选项，跟随 SillyTavern 的连接也可单独选择；流式模式显示当前请求的接收进度。
- 新增全局 Prompt 设置，开头和结尾分别填写并可同时生效；支持变量、暂停启用、持久保存与整库备份。
- 首轮、续写、重roll、补题、自动修复和长记录摘要统一应用当前连接的输出模式和首尾 Prompt。

### Fixed

- 取消所有请求被固定为非流式的限制；旧设置默认保持非流式。
- 流式事件按请求 ID 隔离，并在成功、失败或停止后移除监听；迟到片段不会改变已结束请求。
- 保存 API 设置后显示明确的保存结果。

## [0.1.0-beta.8] - 2026-09-03

### Changed

- 当前记录收敛为“交给他写、重roll、清空重填、生成更多题”四个操作；交换日记的追加按钮显示为“再写一页”。
- 移除完成、归档和重新打开按钮；旧记录的这些状态不再阻止填写或生成。
- 重roll会重新请求最近一次生成，成功后原子替换；清空重填保留各批题目与随题的角色答案；追加新题直接写在当前记录后。
- 双人问卷首轮生成每道题的 Char 答案，复用同一题型、选项和量表范围，与 User 填写区配对展示。
- 逐题评价或批改直接显示在对应答案下方，兼容旧记录中的定向评价。

### Fixed

- 填完后立即生成会先等待手填保存，避免漏掉刚填写的答案；生成期间防止重复提交。
- 重roll失败或停止时保留原内容，停止等待中的请求也不会在保存后继续生成。
- 手机端操作按钮以两列显示，避免末尾按钮被挤出屏幕。
- 手机端每题通过“我的答案 / 他的答案”切换，默认显示我的填写区和对应评价，切换保留输入草稿。

## [0.1.0-beta.7] - 2026-09-02

### Added

- 模板库改为“格式分类 → 内容项”两级结构；分类和内容项均可新增、编辑、删除，点击内容项直接开始。
- 内置格式提供自由主题、依恋类型、星座与相处、关系测验、共同回忆和私密话题等可编辑内容项。
- 设置页增加全局生成上下文，统一管理历史聊天条数、世界书方式、手选条目及长记录预算。
- 当前记录顶部增加“返回记录库”按钮，删除的内置格式可从设置恢复。

### Changed

- 顶部导航改为“模板库、当前记录、记录库、设置”。
- 分类编辑器将玩法流程和核心 JSON 协议分别收纳；日常内容要求使用独立、简化的内容编辑器。
- 悬浮入口由“共”字改为 `✏️` emoji。

## [0.1.0-beta.6] - 2026-09-02

### Added

- 每次开始或继续生成前显示上下文确认页，可开关并设置历史聊天条数、选择当前激活世界书或手选世界书条目，并确认生成连接。
- 生成中的全局状态条增加始终可见的“停止本轮”按钮。

### Fixed

- 加强三种内置玩法和核心 JSON 协议，要求每道题的完整题干直接写入输入卡片并显示，避免只出现“请填写”。
- 兼容模型返回顶层数组、`value`/`text` 字段、缺省输入配置和未转义字符串引号；容错后仍执行 Zod 结构与所有权校验。
- 停止操作立即在本地取消等待并丢弃迟到响应；滚动摘要与主生成改为单次持久化事务，停止或失败不会留下半成品。

## [0.1.0-beta.5] - 2026-09-02

### Fixed

- 修复对 Vue/Pinia 响应式 Proxy 调用 `structuredClone`，导致开始玩法、新建模板、设置页和悬浮按钮点击失败的问题。
- 将悬浮按钮的点击与拖拽结算分离：点击直接开关窗口，只有实际拖动后才保存位置。
- 扩展设置入口改为 SillyTavern 原生折叠栏样式，并增加左下角魔法棒菜单入口。
- 在界面、文档和生成网关测试中明确保证：所有生成结果只进入共笔记录，不新增或修改聊天楼层。

## [0.1.0-beta.4] - 2026-09-02

### Fixed

- 修复生产构建残留 `process.env.NODE_ENV`，导致普通浏览器在挂载入口前抛出 `process is not defined` 的问题。
- 新增直接执行 `dist/index.js` 的发布产物烟雾测试，并禁止浏览器包残留 Node 环境变量引用。

## [0.1.0-beta.3] - 2026-09-02

### Fixed

- 移除会因酒馆助手安装文件夹名不同而阻止整个插件加载的 manifest 硬依赖，改为运行时版本检查。
- 将最低 SillyTavern 版本回退到酒馆助手 4.9.3 同样支持的 1.12.13，避免入口在脚本执行前被隐藏。

## [0.1.0-beta.2] - 2026-09-02

### Fixed

- 悬浮按钮现在首帧就有可见的默认位置，不再等待账户文件初始化。
- 初始化改为后台执行，读取较慢时仍可立即打开界面。
- 在 SillyTavern 扩展设置页增加“打开共笔”备用入口。

## [0.1.0-beta.1] - 2026-09-02

### Added

- “双人问卷”“Char 给 User 的问卷”“交换日记”三种内置模板。
- 五类 User 手填输入、Char 内容与定向评价卡片。
- 独立记录工作区、记录库、模板库和响应式手账界面。
- 跟随 SillyTavern 与多个 OpenAI 兼容独立连接。
- 近期聊天、激活世界书、手选世界书与 token 预算控制。
- JSON Schema 生成、一次自动修复、停止、重试、整轮撤销/重做。
- 账户文件主存储、浏览器待同步草稿、JSON/Markdown 导出与整库备份。
- 长记录滚动摘要和“下一卷”。
